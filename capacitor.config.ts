import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tapan.galagareplica',
  appName: 'Galaga Replica',
  webDir: 'www',
  backgroundColor: '#05070d',
  android: {
    backgroundColor: '#05070d'
  }
};

export default config;
